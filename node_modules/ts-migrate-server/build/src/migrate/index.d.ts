import MigrateConfig from './MigrateConfig';
interface MigrateParams {
    rootDir: string;
    tsConfigDir?: string;
    config: MigrateConfig;
    sources?: string | string[];
}
export default function migrate({ rootDir, tsConfigDir, config, sources, }: MigrateParams): Promise<number>;
export { MigrateConfig };
