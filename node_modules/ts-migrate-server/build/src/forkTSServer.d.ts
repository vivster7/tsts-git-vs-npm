import TSServer from './tsserver/TSServer';
/**
 * Create TSServer with default configuration.
 */
export default function forkTSServer(tssPath?: string): TSServer;
/**
 * Create TSServer without logging.
 * Useful for testing.
 */
export declare function forkTSServerWithNoopLogger(tssPath?: string): TSServer;
