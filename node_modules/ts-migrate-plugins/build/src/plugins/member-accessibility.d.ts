import { Plugin } from 'ts-migrate-server';
declare const accessibility: ("private" | "protected" | "public")[];
declare type Options = {
    defaultAccessibility?: typeof accessibility[number];
    privateRegex?: string;
    protectedRegex?: string;
    publicRegex?: string;
};
declare const memberAccessibilityPlugin: Plugin<Options>;
export default memberAccessibilityPlugin;
