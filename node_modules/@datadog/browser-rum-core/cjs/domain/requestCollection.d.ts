import { Configuration, Duration, RequestType, XhrCompleteContext, XhrStartContext, ClocksState, FetchStartContext, FetchCompleteContext } from '@datadog/browser-core';
import { RumSession } from '..';
import { LifeCycle } from './lifeCycle';
import { TraceIdentifier, Tracer } from './tracing/tracer';
export interface CustomContext {
    requestIndex: number;
    spanId?: TraceIdentifier;
    traceId?: TraceIdentifier;
}
export interface RumFetchStartContext extends FetchStartContext, CustomContext {
}
export interface RumFetchCompleteContext extends FetchCompleteContext, CustomContext {
}
export interface RumXhrStartContext extends XhrStartContext, CustomContext {
}
export interface RumXhrCompleteContext extends XhrCompleteContext, CustomContext {
}
export interface RequestStartEvent {
    requestIndex: number;
}
export interface RequestCompleteEvent {
    requestIndex: number;
    type: RequestType;
    method: string;
    url: string;
    status: number;
    responseText?: string;
    responseType?: string;
    startClocks: ClocksState;
    duration: Duration;
    spanId?: TraceIdentifier;
    traceId?: TraceIdentifier;
    xhr?: XMLHttpRequest;
    response?: Response;
    input?: RequestInfo;
    init?: RequestInit;
    error?: Error;
}
export declare function startRequestCollection(lifeCycle: LifeCycle, configuration: Configuration, session: RumSession): void;
export declare function trackXhr(lifeCycle: LifeCycle, configuration: Configuration, tracer: Tracer): {
    stop: () => void;
};
export declare function trackFetch(lifeCycle: LifeCycle, configuration: Configuration, tracer: Tracer): {
    stop: () => void;
};
